// Minimal config to prevent prettier-plugin-tailwindcss from walking up
// the directory tree. This project uses Tailwind CSS v4 with CSS-based
// configuration (see apps/*/src/index.css).
export default {};
